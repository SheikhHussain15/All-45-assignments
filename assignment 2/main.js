"use strict";
// Assignment # 2
// Storing a person name in a variable
let personName = "Hussain";
// Print a person name in a Lower case
console.log("Lowercase:", personName.toLowerCase());
// Print a person name in a Upper case
console.log("Uppercase:", personName.toUpperCase());
// Print a person name in a Title case
console.log("Titlecase:", personName.charAt(0).toUpperCase() + personName.slice(1).toLowerCase());
